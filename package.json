{
  "name": "mern-storefront",
  "version": "1.0.0",
  "description": "I.EL FAYK — MERN stack portfolio monorepo",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix frontend && npm run build --prefix admin",
    "install:all": "npm i --prefix backend && npm i --prefix frontend && npm i --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.7.0"
  }
}
